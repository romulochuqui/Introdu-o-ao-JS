//O var é utilizado para criar uma variável e impedir que escrevamos muito.
var olaMundo = "Olá Mundo!";
console.log(olaMundo);


//O let é usado como uma var, mas é usada apenas em um bloco. Depois é descartada e fica inexistente.
let romulo = "Rômulo Chuqui Bittencourt da Cunha";
console.log(romulo);


//O const não varia. Como o nome diz, é constante
const money = "CA$H"
console.log(money);

//String é uma "corda de caracteres", uma linha de comando.

//O Array é um objeto global usado na construção de 'arrays': objetos de alto nível semelhantes a listas. 

//As Funções são trechos de códigos que podem ser reutilizados

//O "return" encerra a função e retorna um valor

//DOM: Document Object Model; a estrutura como o objeto é formado







let cor ="verde"; //Nessa linha, se eu escrever "amarelo" no lugar do "verde", por exemplo, a ação mudará para "atenção". 
                  //O comando "switch" (que escrevi no outro documento JS) pode ser usado para substituir os "if"   

if (cor === "verde") {

    console.log("siga");

} else if (cor === "amarelo") {

    console.log("atenção")

} else if (cor = "vermelho") {

    console.log("pare");

}